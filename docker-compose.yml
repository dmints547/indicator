version: "3.9"

services:
  timescaledb:
    image: timescale/timescaledb:latest-pg14
    container_name: timescaledb
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: market
    ports:
      - "5432:5432"
    volumes:
      - timescale_data:/var/lib/postgresql/data
    restart: unless-stopped

  backend:
    image: ${DOCKER_USERNAME}/trading-backend:${BACKEND_TAG:-latest}
    container_name: trading-backend
    environment:
      - MARKETSTACK_API_KEY=${MARKETSTACK_API_KEY}
      - SYMBOLS=${SYMBOLS}
      - POLL_SECONDS=${POLL_SECONDS}
      - CORS_ORIGINS=${CORS_ORIGINS}
      - DATABASE_URL=postgresql+psycopg2://postgres:postgres@timescaledb:5432/market
    ports:
      - "5000:5000"
    depends_on:
      - timescaledb
    restart: unless-stopped

  frontend:
    image: ${DOCKER_USERNAME}/trading-frontend:${FRONTEND_TAG:-latest}
    container_name: trading-frontend
    environment:
      - REACT_APP_API_URL=http://localhost:5000
      - REACT_APP_WS_URL=http://localhost:5000
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  timescale_data:
